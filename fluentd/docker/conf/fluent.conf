
# AUTOMATICALLY GENERATED
# DO NOT EDIT THIS FILE DIRECTLY, USE /templates/conf/fluent.conf.erb

@include kubernetes.conf

<match **>
   @type elasticsearch
   @id out_es
   @log_level info
   include_tag_key true
   host "#{ENV['FLUENT_ELASTICSEARCH_HOST']}"
   port "#{ENV['FLUENT_ELASTICSEARCH_PORT']}"
   scheme "#{ENV['FLUENT_ELASTICSEARCH_SCHEME'] || 'http'}"
   ssl_verify "#{ENV['FLUENT_ELASTICSEARCH_SSL_VERIFY'] || 'false'}"
   user "#{ENV['FLUENT_ELASTICSEARCH_USER']}"
   password "#{ENV['FLUENT_ELASTICSEARCH_PASSWORD']}"
   reload_connections "#{ENV['FLUENT_ELASTICSEARCH_RELOAD_CONNECTIONS'] || 'true'}"
   logstash_prefix "#{ENV['FLUENT_ELASTICSEARCH_LOGSTASH_PREFIX'] || 'logstash'}"
   logstash_format num_threads "#{ENV['FLUENTD_LOGSTASH_FORMAT'] || 'true'}"
   type_name fluentd
   buffer_chunk_limit "#{ENV['FLUENTD_BUFFER_CHUNK_LIMIT'] || '2M'}"
   buffer_queue_limit "#{ENV['FLUENTD_BUFFER_QUEUE_LIMIT'] || '32'}"
   flush_interval "#{ENV['FLUENTD_FLUSH_INTERVAL'] || '5s'}"
   max_retry_wait "#{ENV['FLUENTD_RETRY_WAIT'] || '30'}"
   disable_retry_limit
   num_threads "#{ENV['FLUENTD_FLUSH_THREADS'] || '8'}"
</match>
